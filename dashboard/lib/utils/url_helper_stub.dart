// lib/utils/url_helper_stub.dart
// Stub par défaut — ne devrait jamais être utilisé en pratique
// (remplacé par url_helper_web.dart sur Web, url_helper_io.dart sur natif)

void openInBrowser(String url) {
  throw UnimplementedError(
      'openInBrowser non implémenté pour cette plateforme');
}

void downloadInBrowser(String url) {
  throw UnimplementedError(
      'downloadInBrowser non implémenté pour cette plateforme');
}
